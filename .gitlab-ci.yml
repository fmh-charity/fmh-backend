build-job:
  stage: build
  script:
    - mvn clean install -P test,!dev

test-job:
  stage: test
  script:
    - mvn test -P test, !dev

deploy-prod:
  stage: deploy
  script:
    - echo "This job deploys something from the $CI_COMMIT_BRANCH branch."
